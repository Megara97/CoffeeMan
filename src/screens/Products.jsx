import React from 'react';
import {Text, View, Button} from 'react-native';

const Pay = ({navigation , route}) => {
    return (
        <View>
          <Text>Productos</Text>
        </View>
    );  
};

export default Pay;