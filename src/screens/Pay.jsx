import React from 'react';
import {Text, View, Button} from 'react-native';

const Pay = ({navigation , route}) => {
    return (
        <View>
          <Text>Pago</Text>
        </View>
    );  
};

export default Pay;