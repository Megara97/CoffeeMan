import React from 'react';
import {Text, View, Button} from 'react-native';

const Menu = ({navigation , route}) => {
    return (
        <View>
          <Text>Menu</Text>
        </View>
    );  
};

export default Menu;